{
  "name": "express-markdown-pages",
  "version": "0.1.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "docker:build": "docker build . -t thejimmyg/express-markdown-pages:0.1.0",
    "docker:push": "docker push thejimmyg/express-markdown-pages:0.1.0",
    "docker:build:test": "docker build . -t thejimmyg/express-markdown-pages:test",
    "docker:push:test": "docker push thejimmyg/express-markdown-pages:test",
    "docker:run:local": "docker-compose stop && docker-compose pull && docker-compose up --build -d",
    "docker:stop:local": "docker-compose stop",
    "docker:logs:local": "docker-compose logs --tail='all' -f",
    "start": "node bin/server.js",
    "fix": "standard --fix"
  },
  "author": "James Gardner",
  "license": "MIT",
  "dependencies": {
    "commonmark": "^0.28.1",
    "express": "^4.16.4",
    "express-mustache-jwt-signin": "^0.3.2",
    "express-mustache-overlays": "^0.3.0",
    "yaml-front-matter": "^4.0.0"
  },
  "devDependencies": {
    "standard": "^12.0.1"
  }
}
